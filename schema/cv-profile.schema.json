{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "cv-profile.schema.json",
  "title": "cv-gen profile file",
  "description": "content/profile.yaml, and the untracked content/profile.local.yaml that shallow-overrides its top-level keys. No key is required here, because the override file is partial by design - 'name' is enforced after the merge, at load time, which is the only point where it can be.",
  "type": "object",
  "properties": {
    "name": {
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "object",
          "description": "Per-language text. 'en' is the source; any declared language may be added and falls back to en when absent.",
          "propertyNames": {
            "pattern": "^[a-z]{2}(-[A-Z]{2})?$"
          },
          "additionalProperties": {
            "type": "string"
          },
          "required": [
            "en"
          ],
          "minProperties": 1
        }
      ]
    },
    "anticipated_graduation": {
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "object",
          "description": "Per-language text. 'en' is the source; any declared language may be added and falls back to en when absent.",
          "propertyNames": {
            "pattern": "^[a-z]{2}(-[A-Z]{2})?$"
          },
          "additionalProperties": {
            "type": "string"
          },
          "required": [
            "en"
          ],
          "minProperties": 1
        }
      ],
      "description": "Expected graduation date shown beside the name."
    },
    "contact": {
      "type": "array",
      "items": {
        "oneOf": [
          {
            "type": "string"
          },
          {
            "type": "object",
            "description": "Per-language text. 'en' is the source; any declared language may be added and falls back to en when absent.",
            "propertyNames": {
              "pattern": "^[a-z]{2}(-[A-Z]{2})?$"
            },
            "additionalProperties": {
              "type": "string"
            },
            "required": [
              "en"
            ],
            "minProperties": 1
          }
        ]
      },
      "description": "One markdown line per contact method."
    },
    "tagline": {
      "description": "A string, or a list where the first item passing both gates wins.",
      "oneOf": [
        {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "object",
              "description": "Per-language text. 'en' is the source; any declared language may be added and falls back to en when absent.",
              "propertyNames": {
                "pattern": "^[a-z]{2}(-[A-Z]{2})?$"
              },
              "additionalProperties": {
                "type": "string"
              },
              "required": [
                "en"
              ],
              "minProperties": 1
            }
          ]
        },
        {
          "type": "array",
          "items": {
            "oneOf": [
              {
                "type": "string"
              },
              {
                "type": "object",
                "description": "Per-language text. 'en' is the source; any declared language may be added and falls back to en when absent.",
                "propertyNames": {
                  "pattern": "^[a-z]{2}(-[A-Z]{2})?$"
                },
                "additionalProperties": {
                  "type": "string"
                },
                "required": [
                  "en"
                ],
                "minProperties": 1
              }
            ]
          }
        }
      ]
    }
  },
  "additionalProperties": false
}
